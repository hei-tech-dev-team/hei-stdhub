PORT=3001
DATABASE_URL=postgresql://user@/db_name

JWT_SECRET=change-me-to-a-random-string
CLIENT_URL=http://localhost:5173

# SMTP Gmail — utilisé par Flask-Mail (Python)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre.email@gmail.com
SMTP_PASS=votre-mot-de-passe-application
SMTP_FROM="HEI STDhub <votre.email@gmail.com>"

# Flask-Mail (optionnel, défaut: http://localhost:5050)
FLASKMAIL_URL=http://localhost:5050

# VAPID — notifications push (générées automatiquement si absentes)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
