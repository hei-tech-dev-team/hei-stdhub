name: Keep Backend Alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: curl -sSf -o /dev/null "https://hei-stdhub-api.vercel.app/api/health" || echo "Backend not reachable"
